description: Bad Request Error
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - data
      properties:
        status:
          type: integer
          example: 400
        data:
          type: string
          example: 'Bad Request'
        errors:
          type: string
          example: 'Username/email/password etc. is required'
